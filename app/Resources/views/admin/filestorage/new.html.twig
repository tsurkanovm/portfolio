{% extends 'admin/admin_base.html.twig' %}

{% block content %}
    <h1>New file</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create"/>
    {{ form_end(form) }}

{% endblock %}
